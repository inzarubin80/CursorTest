// Методы для нахождения максимального и минимального значения из массива
//
// Параметры:
//   Массив - Массив значений для анализа
//
// Возвращаемое значение:
//   Структура с полями Максимум и Минимум

// Метод нахождения максимального и минимального значения из массива
Функция МаксимумМинимум(Массив)
	
	Если Не Значение пусто Тогда
		// Инициализация начальных значений
		Максимум = Массив[0];
		Минимум = Массив[0];
		
		// Проход по всем элементам массива
		Для Индекс = 1 По ЧислоЭлементов(Массив) Цикл
			Если Массив[Индекс] > Максимум Тогда
				Максимум = Массив[Индекс];
			КонецЕсли;
			
			Если Массив[Индекс] < Минимум Тогда
				Минимум = Массив[Индекс];
			КонецЕсли;
		КонецЦикла;
		
		// Формирование результата
		Результат = Новый Структура("Максимум, Минимум", Максимум, Минимум);
		Возврат Результат;
	Иначе
		// Возвращаем пустую структуру при пустом массиве
		Возврат Новый Структура("Максимум, Минимум", Неопределено, Неопределено);
	КонецЕсли;
	
КонецФункции

// Пример использования метода
Процедура ПримерИспользования()
	
	// Создание тестового массива
	ТестовыйМассив = Новый Массив;
	ТестовыйМассив.Добавить(5);
	ТестовыйМассив.Добавить(2);
	ТестовыйМассив.Добавить(8);
	ТестовыйМассив.Добавить(1);
	ТестовыйМассив.Добавить(9);
	
	// Вызов метода
	Результат = МаксимумМинимум(ТестовыйМассив);
	
	// Вывод результатов
	Сообщить("Максимум: " + Результат.Максимум);
	Сообщить("Минимум: " + Результат.Минимум);
	
КонецПроцедуры