// Пример модуля 1С (BSL) для тестов MCP-сервера
// Проверяем, что анализ и форматирование работают

Функция ПолучитьДанные(Параметр1, Параметр2)
	Результат = Параметр1 + Параметр2;
	Возврат Результат;
КонецФункции

Процедура ОбработкаПроверкиЗаполнения(Данные, ПроверяемыеРеквизиты, Отказ)
	Если Данные.Пустая() Тогда
		Отказ = Истина;
	КонецЕсли;
КонецПроцедуры